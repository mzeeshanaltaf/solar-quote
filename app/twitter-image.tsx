// Reuse the Open Graph card for the Twitter/X `summary_large_image` slot so the
// declared card actually has an image. Same generator, no duplication.
export { default, alt, size, contentType } from "./opengraph-image";
